{
  "name": "divination",
  "version": "0.0.0",
  "author": "SlashNephy <spica@starry.blue>",
  "repository": "https://github.com/SlashNephy/Divination",
  "license": "MIT",
  "scripts": {
    "build": "yarn workspaces foreach -pi run build",
    "format": "concurrently -m 1 -n format: 'yarn:format:*'",
    "format:eslint": "yarn lint:eslint --fix",
    "format:prettier": "yarn lint:prettier --write",
    "lint": "concurrently -n lint: 'yarn:lint:*'",
    "lint:eslint": "eslint .",
    "lint:prettier": "prettier --check ."
  },
  "devDependencies": {
    "@slashnephy/eslint-config": "2.0.60",
    "@slashnephy/prettier-config": "1.0.42",
    "concurrently": "8.2.2",
    "eslint": "8.53.0",
    "prettier": "3.1.0"
  },
  "packageManager": "yarn@3.7.0",
  "engines": {
    "node": ">=20"
  },
  "eslintConfig": {
    "extends": [
      "@slashnephy/eslint-config"
    ],
    "overrides": [
      {
        "files": "functions/**/*.ts",
        "parserOptions": {
          "project": "./functions/tsconfig.json"
        }
      },
      {
        "files": "packages/PluginRepository/src/**/*.{ts,tsx}",
        "parserOptions": {
          "project": "./packages/PluginRepository/tsconfig.json"
        }
      },
      {
        "files": "packages/PluginRepository/vite.config.ts",
        "parserOptions": {
          "project": "./packages/PluginRepository/tsconfig.node.json"
        }
      }
    ],
    "ignorePatterns": [
      "packages/**/obj/*.json"
    ]
  },
  "workspaces": [
    "functions",
    "packages/*"
  ]
}
