name: CI (AetheryteLinkInChat)

on:
  push:
    paths:
      - AetheryteLinkInChat/**
      - Directory.Build.props
    branches:
      - master
  pull_request:
    paths:
      - AetheryteLinkInChat/**
      - Directory.Build.props
    types:
      - opened
      - synchronize
  release:
    types:
      - created
  workflow_dispatch:

jobs:
  build:
    uses: SlashNephy/Divination/.github/workflows/reusable-build-plugin.yml@5ecb78cb9dbe3892c3776a91b6dcf2cb8eaa9749 # master
    with:
      plugin-name: AetheryteLinkInChat
      working-directory: AetheryteLinkInChat
    permissions:
      contents: read
      actions: write

  deploy:
    if: github.event_name != 'pull_request'
    needs: build
    uses: SlashNephy/Divination/.github/workflows/reusable-deploy-plugin.yml@5ecb78cb9dbe3892c3776a91b6dcf2cb8eaa9749 # master
    with:
      plugin-name: AetheryteLinkInChat
    secrets:
      DEPLOY_GITHUB_TOKEN: ${{ secrets.DEPLOY_GITHUB_TOKEN }}
    permissions:
      actions: read
