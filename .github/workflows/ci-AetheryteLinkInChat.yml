name: CI (AetheryteLinkInChat)

on:
  push:
    paths:
      - AetheryteLinkInChat/**
      - Directory.Build.props
    branches:
      - master
  pull_request:
    paths:
      - AetheryteLinkInChat/**
      - Directory.Build.props
    types:
      - opened
      - synchronize
  release:
    types:
      - created
  workflow_dispatch:

jobs:
  build:
    uses: SlashNephy/Divination/.github/workflows/reusable-build-plugin.yml@09c0ea68d4b07be5dc6c2c95c127c66ed9e8ca15 # master
    with:
      plugin-name: AetheryteLinkInChat
      working-directory: AetheryteLinkInChat
    permissions:
      contents: read
      actions: write

  deploy:
    if: github.event_name != 'pull_request'
    needs: build
    uses: SlashNephy/Divination/.github/workflows/reusable-deploy-plugin.yml@09c0ea68d4b07be5dc6c2c95c127c66ed9e8ca15 # master
    with:
      plugin-name: AetheryteLinkInChat
    secrets:
      DEPLOY_GITHUB_TOKEN: ${{ secrets.DEPLOY_GITHUB_TOKEN }}
    permissions:
      actions: read
