name: CI (FaloopIntegration)

on:
  push:
    paths:
      - FaloopIntegration/**
      - Directory.Build.props
    branches:
      - master
  pull_request:
    paths:
      - FaloopIntegration/**
      - Directory.Build.props
    types:
      - opened
      - synchronize
  release:
    types:
      - created
  workflow_dispatch:

jobs:
  build:
    uses: SlashNephy/Divination/.github/workflows/reusable-build-plugin.yml@ebf8a0614023a1835319c3d0ee162f4c7a2de239 # master
    with:
      plugin-name: FaloopIntegration
      working-directory: FaloopIntegration
    permissions:
      contents: read
      actions: write

  deploy:
    if: github.event_name != 'pull_request'
    needs: build
    uses: SlashNephy/Divination/.github/workflows/reusable-deploy-plugin.yml@ebf8a0614023a1835319c3d0ee162f4c7a2de239 # master
    with:
      plugin-name: FaloopIntegration
    secrets:
      DEPLOY_GITHUB_TOKEN: ${{ secrets.DEPLOY_GITHUB_TOKEN }}
    permissions:
      actions: read
